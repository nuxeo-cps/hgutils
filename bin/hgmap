#!/bin/sh
# inspired from Nuxeo's hgf
# simple mapper on the hg repos found in cwd

for dir in `ls`; do
  if [ -d "$dir"/.hg ]; then
    res=`(cd "$dir" && hg "$@")`
    if [ -n "$res" ]; then
	echo "[$dir]"
	echo "$res"
    fi
  fi
done

